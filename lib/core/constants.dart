import 'package:flutter/cupertino.dart';

const String ID_COLUMN = 'id';

const String NOT_IN__DATABASE_MESSAGE = 'Not in database';
const String ARTIST_ALREADY_EXISTS_MESSAGE = 'Artist already exists';

// * UI Variables *//
const double standardRadiusDouble = 10;
final BorderRadius standardRadius = BorderRadius.circular(standardRadiusDouble);

const double smallPadding = 10;
const double standardPadding = 20;
